<!DOCTYPE html>
<html lang="es" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel de Administración</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome para íconos -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" th:href="@{/css/navbar.css}">
    <link   rel="stylesheet" th:href="@{/css/panel.css}">
</head>
<body>
<header>
    <nav>
        <a th:href="@{/}" class="logo">workana</a>
        <ul>
            <li><a th:href="@{/formularioLogin}" class="btn quiero">Inicia Sección</a></li>

        </ul>
</header>
<div class="container mt-5">
    <h1 class="text-center mb-4 pt-20" style="color: #343a40; font-weight: bold;">Panel de Administración</h1>
    <div class="row">
        <!-- Tarjeta para Clientes -->
        <div class="col-md-4 mb-4">
            <div class="card text-center">
                <div class="card-body">
                    <div class="card-icon">
                        <i class="fas fa-users"></i>
                    </div>
                    <h5 class="card-title">Clientes</h5>
                    <p class="card-text">Gestiona la lista de clientes registrados.</p>
                    <a th:href="@{/clientes}" class="btn btn-custom">Ver Clientes</a>
                </div>
            </div>
        </div>
        <!-- Tarjeta para Proveedores -->
        <div class="col-md-4 mb-4">
            <div class="card text-center">
                <div class="card-body">
                    <div class="card-icon">
                        <i class="fas fa-truck"></i>
                    </div>
                    <h5 class="card-title">Proveedores</h5>
                    <p class="card-text">Gestiona la lista de proveedores registrados.</p>
                    <a th:href="@{/proveedores}" class="btn btn-custom">Ver Proveedores</a>
                </div>
            </div>
        </div>

        <!-- Tarjeta para Contratos -->
        <div class="col-md-4 mb-4">
            <div class="card text-center">
                <div class="card-body">
                    <div class="card-icon">
                        <i class="fas fa-file-contract"></i>
                    </div>
                    <h5 class="card-title">Contratos</h5>
                    <p class="card-text">Gestiona los contratos registrados.</p>
                    <a th:href="@{/contratos}" class="btn btn-custom">Ver Contratos</a>
                </div>
            </div>
        </div>

        <!-- Tarjeta para Reseñas -->
        <div class="col-md-4 mb-4">
            <div class="card text-center">
                <div class="card-body">
                    <div class="card-icon">
                        <i class="fas fa-star"></i>
                    </div>
                    <h5 class="card-title">Reseñas</h5>
                    <p class="card-text">Gestiona las reseñas de clientes.</p>
                    <a th:href="@{/resenas}" class="btn btn-custom">Ver Reseñas</a>
                </div>
            </div>
        </div>

        <!-- Tarjeta para Generar PDF -->
        <div class="col-md-4 mb-4">
            <div class="card text-center">
                <div class="card-body">
                    <div class="card-icon">
                        <i class="fas fa-file-pdf"></i>
                    </div>
                    <h5 class="card-title">Generar PDF</h5>
                    <p class="card-text">Descarga un reporte en PDF.</p>
                    <a th:href="@{/generarPdf}" class="btn btn-custom">Descargar PDF</a>
                </div>
            </div>
        </div>

        <!-- Tarjeta para Prestaciones -->
        <div class="col-md-4 mb-4">
            <div class="card text-center">
                <div class="card-body">
                    <div class="card-icon">
                        <i class="fas fa-concierge-bell"></i>
                    </div>
                    <h5 class="card-title">Prestaciones</h5>
                    <p class="card-text">Gestiona las prestaciones disponibles.</p>
                    <a th:href="@{/prestaciones}" class="btn btn-custom">Ver Prestaciones</a>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
